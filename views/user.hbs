<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>👥 Manage Users</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  {{> navbar}}
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>👥 Manage Users</h1>
      <a href="/users/add" class="btn btn-primary">Add User</a>
    </div>
    <div class="row">
      {{#each users}}
        <div class="col-md-4 mb-4">
          <div class="card text-center">
            <div class="card-body">
              <img src="{{this.avatar}}" class="rounded-circle mb-3" alt="User Avatar" width="100" height="100">
              <h5 class="card-title">{{this.name}}</h5>
              <p class="card-text">{{this.email}}</p>
              <span class="badge {{#if (eq this.role "Admin")}}bg-danger{{else}}bg-secondary{{/if}}">
                {{this.role}}
              </span>
              <div class="d-flex justify-content-between mt-3">
                <a href="/users/edit/{{this.id}}" class="btn btn-warning btn-sm">Edit</a>
                <a href="/users/delete/{{this.id}}" class="btn btn-danger btn-sm">Delete</a>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
  {{> footer}}
</body>
</html>
