<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🥘 Explore Restaurants</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  {{> navbar}}
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>🥘 Explore Restaurants</h1>
      <a href="/restaurants/add" class="btn btn-primary">Add Restaurant</a>
    </div>
    <div class="row">
      {{#each restaurants}}
        <div class="col-md-6 mb-4">
          <div class="card">
            <img src="{{this.image}}" class="card-img-top" alt="Restaurant Image">
            <div class="card-body">
              <h5 class="card-title">{{this.name}}</h5>
              <p class="card-text">Cuisine: {{this.cuisine}}</p>
              <p class="card-text">Rating: {{this.rating}} ⭐</p>
              <p class="card-text">Location: {{this.location}}</p>
              <span class="badge {{#if (eq this.status "Open")}}bg-success{{else}}bg-danger{{/if}}">
                {{this.status}}
              </span>
              <div class="d-flex justify-content-between mt-3">
                <a href="/restaurants/edit/{{this.id}}" class="btn btn-warning btn-sm">Edit</a>
                <a href="/restaurants/delete/{{this.id}}" class="btn btn-danger btn-sm">Delete</a>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
  {{> footer}}
</body>
</html>
